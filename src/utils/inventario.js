// let is_ok = true;
// const inventario = (timeout, tarea) => {
//   return new Promise((resolve, reject) => {
//     setTimeout(() => {
//       if (is_ok) {
//         resolve(tarea);
//       } else {
//         reject("Error");
//       }
//     }, timeout);
//   });
// };

// export default inventario;
